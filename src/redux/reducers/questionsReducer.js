const INITIAL_STATE = {
  response_code: 0,
  results: [],
};

function questionsReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default questionsReducer;
